import { IPath } from './RouterTypes'

export const PATH: IPath = {
  auth: '/auth',
  dashboard: '/dashboard',
  clients: '/clients',
  billing: '/billing',
  notifications: '/notifications',
  admins: '/admins',
  profile: '/profile',
}
