import { IPath } from './RouterTypes'

export const PATH: IPath = {
  auth: '/auth',
  dashboard: '/dashboard',
  clients: '/clients',
  billing: '/billing',
  messages: '/messages',
  admins: '/admins',
  profile: '/profile',
}
